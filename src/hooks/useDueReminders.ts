/**
 * Browser notification for due reminders — DISABLED.
 * Kept as no-op to avoid breaking call sites.
 */
export function useDueReminders(_lang: string, _t?: (key: string) => string) {
  // noop — browser notifications disabled
}
